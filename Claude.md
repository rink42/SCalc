# SCalc - Simple Calc 隨心所欲計算機

## 前提
- **計算機**功能
- 注意 **運算子** 的優先順序 -**(負號)**、(、)、*、/、+、- 

---

## 重點
- 每個數字、運算子 皆可以隨時更改
- 按下 = 直接出現答案
  - 如果運算式有問題，則出現 **!錯誤** (紅字)
  - 每按下一個字即時更新答案
- 對著 = 後的答案按下 **運算子**，把該數值用於下一次運算的第一個數值
  - **!錯誤** 不使用於下一次(使用 **0**)
  - 上一次的運算造成 = 後的數值有變時，下一次的運算也跟著改變
- 按 **掃帚** 圖示清掉一整個運算
  - 不影響它被連動的運算式
  - 點了歷史的運算式，則清除該運算式
- 新的運算式新增在上一個運算式的下方
  - 如果不夠空間，舊運算式往上移
  - 可以用滑鼠/手指壓著拖動檢視歷史記錄

## 執行環境
- 使用 PWA
  - github 帳號是 rink42 密碼 Sgm3tizl
  - 要能被 Chrome / Edge / Bing 新增至主畫面